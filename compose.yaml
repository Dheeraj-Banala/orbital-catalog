services:
  pipeline:
    build: .
    image: orbital-catalog
    env_file: .env
    environment:
      GOOGLE_APPLICATION_CREDENTIALS: /creds/adc.json
    volumes:
      - ./data:/app/data
      - ${GCLOUD_ADC}:/creds/adc.json:ro
